# servidor multienhebrado Echo

Ejemplo de programación de Sockets TCP en Java.

* Crea un hilo al estilo funcional dentro del `Servidor`
* Aprovechamos el mismo cliente que en el cliente Echo monohilo.
